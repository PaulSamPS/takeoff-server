const { sendMsg, setMsgToUnread } = require('../services/message.service')
const { findConnectedUser } = require('../services/room.service')

module.exports = function notificationsHandlers(io, socket) {}
